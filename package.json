{
	"name": "-",
	"version": "0.0.0",
	"description": "",
	"scripts": {
		"copy-lib": "bash _build/copy-lib.sh",
		"autorefresh-lib": "nodemon --watch lib -x \"npm run copy-lib\""
	},
	"devDependencies": {
		"nodemon": "^2.0.7",
		"web-ext": "^6.1.0"
	}
}
