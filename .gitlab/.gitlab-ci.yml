pages:
    stage: deploy
    script:
        - bash build/webdemo/copy-lib.sh
        - mkdir public
        - cp -r webdemo-static/* public
    artifacts:
        paths:
            - public
    only:
        - master